# Environment Configuration

# API Base URL - nur für Standalone Mode notwendig
# Unified Mode (Standard): nicht setzen oder leer lassen (nutzt relative Paths)
# Standalone Mode: VITE_API_BASE=http://localhost:5174
# VITE_API_BASE=

# Enable Vite Proxy für Standalone Mode
# Nur für "npm run dev" + separater "npm run server" notwendig
# VITE_API_PROXY=false

# Server Port (nur für "npm run server")
# PORT=5174

# Job Search Konfiguration
# JOB_CACHE_TTL_MS=300000
# JOB_SEARCH_TIMEOUT_MS=8000
# JOB_SEARCH_MAX_ITEMS=200
